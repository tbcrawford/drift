# Compiled binaries
drift.exe

# OS metadata
.DS_Store

# IDE
.idea/
.vscode/
*.iml

# Test cache / fuzz corpus (testdata/rapid/ is already in place; don't ignore testdata/ entirely)

# goreleaser build output
/dist/

# Go workspace checksum file (generated, not committed; go.work itself is committed)
go.work.sum
