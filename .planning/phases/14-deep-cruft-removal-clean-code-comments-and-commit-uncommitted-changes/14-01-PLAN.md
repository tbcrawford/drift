---
phase: 14-deep-cruft-removal-clean-code-comments-and-commit-uncommitted-changes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .golangci.yml
  - justfile
  - cmd/drift/main_test.go
  - internal/highlight/diff_line.go
  - internal/render/gutter.go
  - internal/render/split_test.go
autonomous: true
requirements:
  - CRUFT-01
must_haves:
  truths:
    - "All 6 pending modified files are committed to git"
    - "go test ./... passes with 0 failures"
    - "golangci-lint v2 config is valid (version: '2' key present)"
    - "justfile has an install recipe"
  artifacts:
    - path: ".golangci.yml"
      provides: "golangci-lint v2 compatible config"
      contains: 'version: "2"'
    - path: "justfile"
      provides: "install recipe"
      contains: "install:"
    - path: "internal/render/gutter.go"
      provides: "gutterColumnSeparator constant + dim/highlight fg split"
      contains: "gutterColumnSeparator"
  key_links:
    - from: ".golangci.yml"
      to: "golangci-lint run ./..."
      via: "version: 2 key selects v2 schema"
      pattern: 'version: "2"'
---

<objective>
Commit the 6 pending modified files that accumulated during phases 11–13 without being staged.

Purpose: These changes are already working (219 tests pass, go vet clean), just unstaged. Phase 14's first job is to make git history match reality.
Output: 6 files committed in one clean commit; working tree is clean for plan 02.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify working state and stage pending files</name>
  <files>.golangci.yml, justfile, cmd/drift/main_test.go, internal/highlight/diff_line.go, internal/render/gutter.go, internal/render/split_test.go</files>
  <action>
    Before staging, confirm the project is in a clean state:
    1. Run `go test ./...` — must pass all 219 tests
    2. Run `go build ./...` — must succeed
    3. Run `go vet ./...` — must produce no output

    Then review what each file contains:
    - `.golangci.yml` — migrated to golangci-lint v2 schema (added `version: "2"` top-level key, restructured `exclusions` under `linters:` and `formatters:` blocks per v2 migration guide, removed `gosimple` linter which was removed from v2)
    - `justfile` — added `install` recipe: `go install ./cmd/drift`
    - `cmd/drift/main_test.go` — added `"--no-line-numbers"` to the flags-in-help smoke test
    - `internal/highlight/diff_line.go` — `DiffLineStyle` return type changed from `lipgloss.Style` to `chroma.Colour`; removed `ApplyDiffLineStyle` (inlined at call sites); comment updated to reference `DiffLineBackgroundColour` (not the removed `DiffLineMutedBackgroundColour`)
    - `internal/render/gutter.go` — extracted `gutterColumnSeparator = " │"` constant; split foreground into `dim` (context rows) and `high` (delete/insert rows); added `styledGutterColumnSeparator` helper
    - `internal/render/split_test.go` — updated `TestSplit_NoColorSeparator` to expect `│` separator (not ASCII `|`); added `TestSplit_WordDiffPairedDeleteInsert`

    Stage all 6 files:
    ```
    git add .golangci.yml justfile cmd/drift/main_test.go internal/highlight/diff_line.go internal/render/gutter.go internal/render/split_test.go
    ```
  </action>
  <verify>
    <automated>go test ./... && go build ./... && go vet ./... && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>All three commands exit 0 and "ALL CHECKS PASS" appears in output</done>
</task>

<task type="auto">
  <name>Task 2: Commit accumulated changes</name>
  <files>.golangci.yml, justfile, cmd/drift/main_test.go, internal/highlight/diff_line.go, internal/render/gutter.go, internal/render/split_test.go</files>
  <action>
    Create one commit with a clear multi-scope message covering all 6 files:

    ```
    git commit -m "chore(phase-14): commit accumulated working-tree changes

    - golangci-lint: migrate to v2 schema (version key, restructured exclusions)
    - justfile: add install recipe (go install ./cmd/drift)
    - cmd/drift: cover --no-line-numbers flag in help smoke test
    - highlight: DiffLineStyle returns chroma.Colour; remove ApplyDiffLineStyle
    - render/gutter: extract gutterColumnSeparator const; split dim/high fg
    - render/split_test: update NoColor separator assertion; add word-diff test"
    ```

    After commit, verify `git status` shows a clean working tree (only untracked .planning/phases/14-* remains).
  </action>
  <verify>
    <automated>git diff --cached --stat && git status --short</automated>
  </verify>
  <done>git status shows no modified tracked files; the commit appears in `git log --oneline -1`</done>
</task>

</tasks>

<verification>
- `go test ./...` passes (219 tests, 0 failures)
- `git log --oneline -1` shows the new commit
- `git status --short` shows only untracked phase-14 planning files
- `.golangci.yml` contains `version: "2"` at the top
- `justfile` contains an `install:` recipe
</verification>

<success_criteria>
All 6 pending modified files are committed. Working tree has no modified tracked files. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/14-deep-cruft-removal-clean-code-comments-and-commit-uncommitted-changes/14-01-SUMMARY.md`
</output>
