---
phase: 14-deep-cruft-removal-clean-code-comments-and-commit-uncommitted-changes
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - internal/highlight/diffcolors.go
autonomous: true
requirements:
  - CRUFT-02
must_haves:
  truths:
    - "DiffLineMutedBackgroundColour is removed from diffcolors.go"
    - "go test ./... passes with 0 failures after removal"
    - "go build ./... succeeds after removal"
    - "No references to DiffLineMutedBackgroundColour remain in any .go file"
  artifacts:
    - path: "internal/highlight/diffcolors.go"
      provides: "clean diffcolors without dead exported function"
  key_links:
    - from: "internal/highlight/diffcolors.go"
      to: "internal/render/wordline.go"
      via: "WordSpanBackgroundColour (still exported, still called)"
      pattern: "WordSpanBackgroundColour"
---

<objective>
Remove the dead exported function `DiffLineMutedBackgroundColour` from `internal/highlight/diffcolors.go`.

Purpose: This function was superseded when `DiffLineStyle` was refactored to use `DiffLineBackgroundColour` directly (terrasort parity). No callers exist outside the definition file itself. Removing it shrinks the internal API surface and eliminates a confusing "muted plane" concept that no longer reflects how rendering works.
Output: `diffcolors.go` with `DiffLineMutedBackgroundColour` removed; all tests still pass.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key functions in diffcolors.go — callers and their status -->

KEEP (has callers):
- `DiffLineBackgroundColour(style, isDark, del)` — called from diff_line.go, unified.go, split.go, wordline.go
- `GutterBackgroundHex(isDark, oldSide)` — called from gutter.go
- `GutterDimForegroundHex(isDark)` — called from gutter.go, styledGutterColumnSeparator
- `GutterHighlightForegroundHex(isDark)` — called from gutter.go
- `LineFallbackFromTerminalRGB(r,g,b,isDark,del)` — called from DiffLineBackgroundColour
- `WordSpanBackgroundColour(style, isDark, del)` — called from wordline.go, gutter.go, tests
- `terminalBaseRGB(isDark)` — called from DiffLineMutedBackgroundColour AND WordSpanBackgroundColour indirectly via blendColourTowardRGB
- `blendColourTowardRGB(c, tr,tg,tb, alpha)` — called from DiffLineMutedBackgroundColour AND WordSpanBackgroundColour

REMOVE (zero callers outside definition file):
- `DiffLineMutedBackgroundColour(style, isDark, del)` — lines 185-196 of diffcolors.go
  IMPORTANT: `terminalBaseRGB` and `blendColourTowardRGB` are also called by `WordSpanBackgroundColour`
  so they must be KEPT even after removing DiffLineMutedBackgroundColour.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove DiffLineMutedBackgroundColour from diffcolors.go</name>
  <files>internal/highlight/diffcolors.go</files>
  <action>
    Remove the dead exported function `DiffLineMutedBackgroundColour` and its doc comment from `internal/highlight/diffcolors.go`. The function occupies lines 185–196 (including the comment block starting at 185).

    Specifically, delete these lines from diffcolors.go:
    ```go
    // DiffLineMutedBackgroundColour returns [DiffLineBackgroundColour] blended further
    // toward the terminal base (42%). Full-line rendering uses [DiffLineBackgroundColour]
    // directly (terrasort parity); this helper remains for tests or callers that want an
    // extra-muted plane.
    func DiffLineMutedBackgroundColour(style *chroma.Style, isDark, del bool) chroma.Colour {
    	base := DiffLineBackgroundColour(style, isDark, del)
    	if !base.IsSet() {
    		return chroma.Colour(0)
    	}
    	tr, tg, tb := terminalBaseRGB(isDark)
    	return blendColourTowardRGB(base, tr, tg, tb, 0.42)
    }
    ```

    DO NOT remove:
    - `terminalBaseRGB` — still called by blendColourTowardRGB chain via WordSpanBackgroundColour
    - `blendColourTowardRGB` — called by WordSpanBackgroundColour (lines 207, 209)

    After editing, verify no blank lines accumulate between `clampFloat` and `terminalBaseRGB`.

    Confirm with grep: `grep -n "DiffLineMutedBackgroundColour" internal/highlight/diffcolors.go` must return no output.
  </action>
  <verify>
    <automated>grep -rn "DiffLineMutedBackgroundColour" --include="*.go" . && echo "FOUND — must be empty" || echo "CLEAN — function removed"</automated>
  </verify>
  <done>grep returns no matches (exits 1 = "CLEAN" message printed); diffcolors.go no longer exports DiffLineMutedBackgroundColour</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean build + tests, then commit</name>
  <files>internal/highlight/diffcolors.go</files>
  <action>
    1. Run `go build ./...` — must succeed (no "undefined: DiffLineMutedBackgroundColour" errors)
    2. Run `go test ./...` — must pass all tests
    3. Run `go vet ./...` — must produce no output

    If all pass, stage and commit:
    ```
    git add internal/highlight/diffcolors.go
    git commit -m "refactor(highlight): remove dead DiffLineMutedBackgroundColour

    DiffLineStyle was refactored to call DiffLineBackgroundColour directly
    (terrasort parity in phase 11). DiffLineMutedBackgroundColour has had zero
    callers since that refactor. Remove the export to shrink internal API surface."
    ```

    Then update ROADMAP.md to mark phase 14 progress and commit planning artifacts.
  </action>
  <verify>
    <automated>go test ./... && go build ./... && go vet ./... && echo "ALL CLEAN"</automated>
  </verify>
  <done>All commands exit 0; "ALL CLEAN" appears; git log shows the refactor commit</done>
</task>

</tasks>

<verification>
- `grep -rn "DiffLineMutedBackgroundColour" --include="*.go" .` returns 0 matches
- `go test ./...` passes (same count as before, no regressions)
- `go build ./...` succeeds
- `WordSpanBackgroundColour`, `terminalBaseRGB`, `blendColourTowardRGB` are still present in diffcolors.go
</verification>

<success_criteria>
`DiffLineMutedBackgroundColour` is removed. No test regressions. Project builds and all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/14-deep-cruft-removal-clean-code-comments-and-commit-uncommitted-changes/14-02-SUMMARY.md`
</output>
