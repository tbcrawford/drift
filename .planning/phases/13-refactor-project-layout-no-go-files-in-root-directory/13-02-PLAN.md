---
phase: 13-refactor-project-layout-no-go-files-in-root-directory
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - README.md
  - drift/doc.go
  - .gitignore
autonomous: true
requirements:
  - LAYOUT-04

must_haves:
  truths:
    - "README.md shows the correct new import path github.com/tylercrawford/drift/drift"
    - "Compiled drift binary at root is gitignored"
    - "doc.go import path example is correct"
  artifacts:
    - path: ".gitignore"
      provides: "Ignores compiled drift binary and build artifacts at root"
      contains: "drift"
    - path: "README.md"
      provides: "Updated installation and usage docs with new import path"
    - path: "drift/doc.go"
      provides: "Package doc with correct import path in example"
  key_links:
    - from: "README.md"
      to: "github.com/tylercrawford/drift/drift"
      via: "go get instruction and code example"
      pattern: "tylercrawford/drift/drift"
---

<objective>
Update user-facing documentation (README.md, doc.go) to reflect the new library import path `github.com/tylercrawford/drift/drift`, and add `.gitignore` to prevent the compiled binary and build artifacts from appearing in git status.

Purpose: Complete the refactor — users reading the README and godoc get the correct import path; the repo is clean with no stray build artifacts.

Output:
- `.gitignore` file ignoring the `drift` binary at root
- `README.md` with updated import path example
- `drift/doc.go` with updated import path in package example
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .gitignore to ignore compiled binary and build artifacts</name>
  <files>.gitignore</files>
  <action>
Create `.gitignore` at the module root. The project currently has NO `.gitignore` file, which means the compiled `drift` binary (built by `go build .` or `go build ./cmd/drift/...`) and other build artifacts appear as untracked files.

Content for `.gitignore`:

```
# Compiled binaries
drift
drift.exe

# OS metadata
.DS_Store

# IDE
.idea/
.vscode/
*.iml

# Test cache / fuzz corpus (testdata/rapid/ is already in place; don't ignore testdata/ entirely)
```

Do NOT gitignore `testdata/` — it contains the rapid fuzz corpus which should be tracked.
The `drift` binary at root (the result of `go build .` before the refactor, or now just `go build .` which builds nothing since there's no main at root) should be ignored.

After creating, verify: `git status` should show `.gitignore` as a new file and the `drift` binary should no longer show as untracked.
  </action>
  <verify>
    <automated>test -f .gitignore && grep -q "^drift$" .gitignore</automated>
  </verify>
  <done>.gitignore exists with `drift` binary listed; git status no longer shows untracked `drift` binary</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and drift/doc.go with new import path</name>
  <files>README.md, drift/doc.go</files>
  <action>
Update the import path references in documentation from `github.com/tylercrawford/drift` to `github.com/tylercrawford/drift/drift`.

**README.md changes:**

Line 10: `go get github.com/tylercrawford/drift@latest`
→ `go get github.com/tylercrawford/drift/drift@latest`

Line 40 (in the library functional API code example):
```go
"github.com/tylercrawford/drift"
```
→
```go
"github.com/tylercrawford/drift/drift"
```

Do NOT change line 11 (`go install github.com/tylercrawford/drift/cmd/drift@latest`) — that is the CLI install command for the cmd package, not the library import path. It is already correct.

**drift/doc.go changes (if it has an import example):**

Check for any import path example in drift/doc.go. If it contains `"github.com/tylercrawford/drift"` as an example import, update it to `"github.com/tylercrawford/drift/drift"`. The module-level import in doc.go's text/examples (not in actual Go code — doc.go has `package drift` at top, no `import` statement) should be updated.

Read drift/doc.go first and update any import path string that appears in comments or examples. The `package drift` declaration stays unchanged.

After updates:
```bash
grep "github.com/tylercrawford/drift" README.md drift/doc.go
```
Should show ONLY:
- `github.com/tylercrawford/drift/drift` (import path for library users)
- `github.com/tylercrawford/drift/cmd/drift` (CLI install command — already correct)
No bare `"github.com/tylercrawford/drift"` (without `/drift` or `/cmd`) should remain.
  </action>
  <verify>
    <automated>grep -c '"github.com/tylercrawford/drift"' README.md drift/doc.go 2>/dev/null | grep -v ":0" | wc -l | grep "^0$"</automated>
  </verify>
  <done>No bare github.com/tylercrawford/drift (without subpackage) remains in README.md or drift/doc.go; correct import path shown in examples</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `test -f .gitignore` → file exists
2. `git status` → `drift` binary not shown as untracked
3. `grep '"github.com/tylercrawford/drift"' README.md` → 0 results (only /drift or /cmd/drift variants remain)
4. `go doc github.com/tylercrawford/drift/drift` → renders package docs cleanly
5. `go build ./...` → still passes
6. `go test ./...` → still passes
</verification>

<success_criteria>
- `.gitignore` exists with `drift` binary entry
- `README.md` shows `go get github.com/tylercrawford/drift/drift@latest` and `"github.com/tylercrawford/drift/drift"` import
- No stale bare `"github.com/tylercrawford/drift"` import paths remain in documentation
- Full test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactor-project-layout-no-go-files-in-root-directory/13-02-SUMMARY.md`
</output>
