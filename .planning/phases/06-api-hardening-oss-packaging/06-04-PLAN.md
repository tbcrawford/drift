---
plan: "06-04"
title: "Benchmarks — 10k-line unified and split render"
wave: 2
depends_on: []
files_modified:
  - "benchmark_test.go"
requirements_addressed: ["OSS-07"]
autonomous: true
---

## Objective

Add Go benchmarks that diff and render two ~10,000-line strings in both unified and split modes, using `testing.B` and deterministic in-memory writers. Satisfy OSS-07 and ROADMAP success criterion #4 (under 1 second per iteration on a typical dev machine — encode acceptance as a conservative `b.N` loop with documented local check).

## must_haves

- [ ] Benchmark functions live in root `drift` test package (e.g. `benchmark_test.go`)
- [ ] Sub-benchmarks or separate `Benchmark*` entries for unified vs split
- [ ] No filesystem I/O inside the timed loop

## Tasks

<task id="06-04-01" title="Add benchmark_test.go with 10k-line generator and Render">
<read_first>
- drift.go — `Diff`
- render.go — `Render` + options
- internal/render/termwidth.go — behavior for non-TTY `io.Writer` (split width)
</read_first>

<action>
**Step 1:** Add `benchmark_test.go` with helper `func generateLines(n int) (old, new string)` that builds two large strings sharing most lines and a middle hunk of edits (simplest: base template line `fmt.Sprintf("line %08d\n", i)` for i in 0..n-1, then `new` replaces a slice of lines in the middle).

**Step 2:** Define benchmarks (names are illustrative — use exact names below for acceptance):

- `BenchmarkDiff10k` — loop: `Diff(old, new, WithAlgorithm(Myers))` (or default)
- `BenchmarkRenderUnified10k` — setup `result` once with `b.ResetTimer`; loop: `Render(result, &buf, WithNoColor())` into `bytes.Buffer` that is reset or reallocated per documentation needs (prefer `Truncate(0)` + `Seek(0,0)` or new buffer if simpler)
- `BenchmarkRenderSplit10k` — same as unified but `Render(..., WithNoColor(), WithSplit())`

**Step 3:** In file comment, document: "Expect each benchmark iteration &lt;1s on modern laptop; use `go test -bench=. -benchtime=100ms` to smoke test."

**Step 4:** Run `go test -bench=. -run '^$' -count=1 ./.` from repo root (package root) and ensure benchmarks compile and run.
</action>

<acceptance_criteria>
- `grep -q 'func BenchmarkDiff10k' benchmark_test.go` exits 0
- `grep -q 'func BenchmarkRenderUnified10k' benchmark_test.go` exits 0
- `grep -q 'func BenchmarkRenderSplit10k' benchmark_test.go` exits 0
- `grep -q '10000' benchmark_test.go` exits 0 (or `10_000` — line count constant must appear)
- `go test -bench=BenchmarkDiff10k -benchtime=1x -run '^$' .` exits 0 (run from module root targeting `.` if benchmarks are in root package)
</acceptance_criteria>
</task>

## Verification criteria

- Bench code uses `WithNoColor()` to minimize environment variance
- Split benchmark actually passes `WithSplit()` into `Render`
