# Plan 10-01 — Theme-aware full-line diff styling

## Objective

Add theme-derived full-line backgrounds for `+`/`-` lines in unified and split renderers using Chroma `GenericDeleted` / `GenericInserted`, ANSI-safe wrapping, and `WithLineDiffStyle` (default on).

## Tasks

- [x] `internal/highlight/diff_line.go` — `DiffLineStyle`, `ApplyDiffLineStyle`
- [x] `internal/render/unified.go` — apply to `prefix + highlighted` when `LineDiffStyle && !NoColor`
- [x] `internal/render/split.go` — `splitApplyDiffLine` for delete (left) / insert (right)
- [x] `options.go`, `render.go`, `builder.go` — `lineDiffStyle` default true, `WithLineDiffStyle`
- [x] Tests — `diff_line_test.go`, existing render tests unchanged (noop config keeps `LineDiffStyle` false)

## Verification

- `go test ./...`
- `just lint`
