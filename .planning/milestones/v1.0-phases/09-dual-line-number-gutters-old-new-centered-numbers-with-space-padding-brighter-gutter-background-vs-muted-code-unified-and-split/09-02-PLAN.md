---
plan: "09-02"
title: "Split renderer gutters + integration tests + README"
wave: 2
depends_on:
  - "09-01"
files_modified:
  - "internal/render/split.go"
  - "internal/render/split_test.go"
  - "render_test.go"
  - "README.md"
  - "builder.go"
requirements_addressed: []
autonomous: true
---

## Objective

Extend **split** rendering so each panel includes its **line-number gutter** (old left, new right), re-deriving **panel content width** so total output fits `TermWidth`. Reuse `gutter.go` helpers and `ShowLineNumbers` / `IsDark` from `RenderConfig`. Add **`Builder`** chaining if `WithoutLineNumbers` exists. Update **README** with one example showing line-number columns. Depends on **09-01** (gutter primitives + unified + options).

## must_haves

- [ ] `go test ./... -count=1` exits 0
- [ ] Split view with `ShowLineNumbers: false` matches pre-phase-9 split layout (no extra columns)
- [ ] README mentions line-number gutters and `WithLineNumbers` / opt-out

## Tasks

<task id="9-02-01" title="Split renderer: gutters inside each panel">
<read_first>
- internal/render/split.go
- internal/render/gutter.go (from 09-01)
- internal/render/unified.go — `RenderConfig` fields
</read_first>

<action>
**Step 1:** When `cfg.ShowLineNumbers` is false, keep existing `Split()` behavior (no structural changes to line assembly).

**Step 2:** When `ShowLineNumbers` is true:

- For each hunk, compute **pair** list via existing `pairHunkLines`.
- For the **set of pairs** in that hunk, derive line numbers for left gutter from paired deletes/equals: use `edittype.Line` reconstructed from pairing — **note:** `linePair` currently stores only `Content` strings; executor must **thread `OldNum`/`NewNum`** into `linePair` by extending `linePair` struct with `leftOldNum`, `rightNewNum` (or carry `edittype.Line` references). Update `pairHunkLines` to populate numbers from the original `edittype.Line` entries so gutters show correct file line numbers for each row.

**Step 3:** Compute `oldW, newW` across **all pairs** in the hunk (scan paired line numbers).

**Step 4:** For each pair row: format `leftGutter = centerLineNumber(leftNum, oldW)` with old gutter style; `rightGutter = centerLineNumber(rightNum, newW)` with new gutter style. Apply `leftStyle` / `rightStyle` to **code only** after gutters: `leftBlockLine = leftGutterRendered + leftStyle.Render(lContent)` (adjust width: `leftStyle` width = `panelWidth - oldW - sepInner` where `sepInner` is 0 or 1 if gutter touches code without extra `|`).

**Step 5:** Recalculate `panelWidth` from `termWidth`:  
`innerLeft = panelWidth - oldGutterWidth - 1` (if padding char between gutter and code), matching research: subtract gutter widths from each half **after** computing `oldW`/`newW` **per hunk**.

**Step 6:** Preserve ` │ ` column separator between **full left block** and **full right block** (gutter+code included in each block).

</action>

<acceptance_criteria>
- `grep -q 'ShowLineNumbers' internal/render/split.go` exits 0
- `grep -qE 'OldNum|NewNum' internal/render/split.go` exits 0 OR `linePair` struct in split.go contains fields for line numbers
- `go test ./internal/render/... -count=1` exits 0
</acceptance_criteria>
</task>

<task id="9-02-02" title="Tests + drift.Render integration + builder">
<read_first>
- internal/render/split_test.go
- render_test.go
- builder.go
</read_first>

<action>
**Step 1:** Extend `split_test.go` with a hunk fixture where old/new line numbers differ; assert substrings for both gutters when `ShowLineNumbers` true.

**Step 2:** Add `render_test.go` tests: `TestRender_WithLineNumbers_Default` (or similar) asserting unified output contains numeric gutters for a trivial diff; `TestRender_WithoutLineNumbers` asserting no gutter column pattern.

**Step 3:** If `WithoutLineNumbers()` added in 09-01, add `Builder` method `WithoutLineNumbers() *Builder` mirroring `Split()` pattern.

**Step 4:** Update `README.md` in **Rendering** (or equivalent section): one sentence + code snippet: `drift.WithLineNumbers(true)` default and opt-out.

</action>

<acceptance_criteria>
- `go test ./... -run TestRender -count=1` exits 0
- `grep -q 'LineNumber' README.md` exits 0 OR README contains substring `line number`
- `go test ./... -count=1` exits 0
</acceptance_criteria>
</task>

## Verification criteria

- Executor reads `pairHunkLines` carefully — **must** preserve correct `OldNum`/`NewNum` per visual row (may require struct expansion; document in SUMMARY if pairing logic changes)

## PLANNING COMPLETE
