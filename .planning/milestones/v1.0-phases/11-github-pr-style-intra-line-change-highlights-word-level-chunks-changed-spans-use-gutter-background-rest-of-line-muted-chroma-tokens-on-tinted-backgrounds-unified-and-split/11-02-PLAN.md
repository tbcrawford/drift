# Plan 11-02 — Gap closure: render integration for word diff

## Objective

Wire `internal/worddiff.PairSegments` into unified and split renderers with muted vs gutter-tinted spans, `WithWordDiff` (default on), and tests.

## Tasks

- [x] `internal/render/wordline.go` — `highlightSegmented`, `splitHighlightPair`, `unifiedHighlightPair`, `shouldWordDiffPair` (profile allowlist: TrueColor / ANSI256 / ANSI)
- [x] `internal/render/gutter.go` — `gutterTintStyle` for changed spans
- [x] `split.go` / `unified.go` — `RenderConfig.WordDiff`
- [x] `options.go`, `render.go`, `builder.go` — `WithWordDiff`
- [x] Tests — `TestUnified_WordDiffPairedDeleteInsert`, `TestSplit_WordDiffPairedDeleteInsert`

## Verification

- `go test ./...`
- `just lint`
