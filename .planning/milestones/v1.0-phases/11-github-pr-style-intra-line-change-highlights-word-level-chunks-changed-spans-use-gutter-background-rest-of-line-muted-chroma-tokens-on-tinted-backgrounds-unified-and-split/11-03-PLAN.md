---
wave: 1
depends_on: []
files_modified:
  - internal/highlight/diffcolors.go
  - internal/highlight/diff_line.go
  - internal/render/gutter.go
  - internal/render/wordline.go
  - internal/render/unified.go
  - internal/render/split.go
  - internal/highlight/*_test.go
  - internal/render/*_test.go
autonomous: true
requirements: []
---

# Plan 11-03 — Gap closure: terrasort color parity + GitHub PR layering

## Objective

Make drift’s **line** and **intra-line word** colors match **terrasort**’s `DeriveUXTheme` / `diffcolors` roles (`AddBg`, `RemoveBg`, gutter neutrals for intra-line emphasis), while keeping **`internal/worddiff`** segmentation unchanged. Ensure **full-line** semantic background wraps the **entire** paired `-`/`+` row (prefix + body) in unified and both panels in split when `LineDiffStyle` is on, matching **D-LAYER-01/02** and **D-COLOR-01–03** in `11-CONTEXT.md`.

## must_haves

1. `DiffLineBackgroundColour` (or equivalent) produces the **same RGB intent** as terrasort `chromaDiffLineRGBA` for the same `*chroma.Style`, `isDark`, and del flag — including `GenericInserted`/`GenericDeleted` handling, `blendChromaTowardTerminalBase`, and `fallbackDiffRGBA` fallbacks (port logic from `/Users/tylercrawford/dev/github/terrasort/internal/highlight/diffcolors.go`).
2. Intra-line **changed** spans use **neutral gutter** backgrounds only (`GutterBackgroundHex` / same RGB as terrasort `driftStyleGutterRGBA`), **not** semantic red/green on the word span alone.
3. Word-diff code paths apply **full-line** `ApplyDiffLineStyle` / `splitApplyDiffLine` **after** segmented highlight so the whole line carries add/remove color.
4. `go test ./...` and `just lint` pass.

## Tasks

<task id="11-03-T1">
<title>Port terrasort chroma line colour pipeline into drift diffcolors</title>
<read_first>
- `/Users/tylercrawford/dev/github/terrasort/internal/highlight/diffcolors.go`
- `internal/highlight/diffcolors.go`
- `internal/highlight/diff_line.go`
- `.planning/phases/11-github-pr-style-intra-line-change-highlights-word-level-chunks-changed-spans-use-gutter-background-rest-of-line-muted-chroma-tokens-on-tinted-backgrounds-unified-and-split/11-CONTEXT.md` (D-COLOR-01)
</read_first>
<action>
Replace or refactor `DiffLineBackgroundColour` in `internal/highlight/diffcolors.go` so its behavior matches terrasort’s `chromaDiffLineRGBA` + `diffEntryChromaColour` + `blendChromaTowardTerminalBase` + `fallbackDiffRGBA` (same token types `GenericInserted`/`GenericDeleted`, same fallbacks `#3a2228`/`#243520`/`#ffeaea`/`#e6f7e6`). Export or keep private helpers as needed; `diff_line.go` must call the unified entry point. Remove any drift-only “blend theme Background toward pure green/red” path if it conflicts with this parity requirement.
</action>
<acceptance_criteria>
- `internal/highlight/diffcolors.go` contains functions logically equivalent to terrasort: use of `chroma.GenericDeleted` / `chroma.GenericInserted`, `e.Background` then `e.Colour` with blend toward terminal base `(18,18,22)` vs `(255,255,255)` at mix `0.78`, and the same fallback hexes as terrasort `fallbackDiffRGBA`.
- `go test ./internal/highlight/...` exits 0.
- Grep: `DiffLineBackgroundColour` is still the public API used by `diff_line.go`.
</acceptance_criteria>
</task>

<task id="11-03-T2">
<title>Lock intra-line tint to gutter neutrals only</title>
<read_first>
- `internal/render/gutter.go`
- `/Users/tylercrawford/dev/github/terrasort/internal/highlight/diffcolors.go` (`driftStyleGutterRGBA`)
- `11-CONTEXT.md` (D-COLOR-03)
</read_first>
<action>
Ensure `gutterTintStyle(isDark, noColor, oldSide)` in `internal/render/gutter.go` sets background **only** from `highlight.GutterBackgroundHex(isDark, oldSide)` (RGB aligned with terrasort `driftStyleGutterRGBA`: dark old `#585858`, dark new `#444444`, light old `#e4e4e4`, light new `#eeeeee`). It must **not** call `DiffLineBackgroundColour`. Signature stays `gutterTintStyle(isDark, noColor, oldSide bool)` without `*chroma.Style`.
</action>
<acceptance_criteria>
- `grep -n DiffLineBackgroundColour internal/render/gutter.go` returns no matches.
- `gutterTintStyle` body contains `GutterBackgroundHex`.
- `go test ./internal/render/...` exits 0.
</acceptance_criteria>
</task>

<task id="11-03-T3">
<title>Verify full-line wrap on word-diff unified and split</title>
<read_first>
- `internal/render/unified.go`
- `internal/render/wordline.go`
- `internal/render/split.go`
</read_first>
<action>
Confirm paired delete/insert with `WordDiff` and `LineDiffStyle`: unified applies `ApplyDiffLineStyle` to `-`+body and `+`+body after `unifiedHighlightPair`; split’s `splitHighlightPair` returns `splitApplyDiffLine(...)` for word-diff branch. If either path is missing, add the wrap. Do not change `PairSegments` behavior.
</action>
<acceptance_criteria>
- `unified.go` contains `ApplyDiffLineStyle` inside the `unifiedHighlightPair` success branch when `LineDiffStyle && !NoColor`.
- `wordline.go` `splitHighlightPair` word-diff branch ends with `return splitApplyDiffLine(cfg, style, pair, l, r)`.
- `go test ./internal/render/...` exits 0.
</acceptance_criteria>
</task>

<task id="11-03-T4">
<title>Regression tests for color roles</title>
<read_first>
- `internal/highlight/diffcolors_test.go`
- `internal/render/gutter_test.go`
- `internal/render/unified_test.go`
</read_first>
<action>
Add or extend tests: (1) monokai/github-dark: insert line colour has green bias, delete has red bias after T1. (2) `gutterTintStyle` rendered output for light theme includes `e4e4e4` or equivalent neutral for old column. (3) Optional: word-diff unified output contains background SGR `;48;` for full line. Keep tests hermetic (no network).
</action>
<acceptance_criteria>
- `go test ./internal/highlight/... ./internal/render/...` exits 0.
- At least one new `Test*` name includes `Terrasort` or `GutterNeutral` or `FullLineWordDiff`.
</acceptance_criteria>
</task>

<task id="11-03-T5">
<title>Update phase verification doc</title>
<read_first>
- `.planning/phases/11-github-pr-style-intra-line-change-highlights-word-level-chunks-changed-spans-use-gutter-background-rest-of-line-muted-chroma-tokens-on-tinted-backgrounds-unified-and-split/11-VERIFICATION.md`
</read_first>
<action>
Append a subsection under Checks: terrasort parity for line RGB pipeline; gutter-only neutral for changed words; full-line wrap on word-diff. Mark human_verification to compare drift vs terrasort side-by-side in a true-color terminal.
</action>
<acceptance_criteria>
- `11-VERIFICATION.md` references plan 11-03 and lists the three checks above as unchecked until human sign-off.
- File saved under phase directory.
</acceptance_criteria>
</task>

## Verification

- `go test ./...`
- `just lint`
- Manual: `drift` / library render on a small paired change; colors should match terrasort roles for line vs gutter-word emphasis.
