---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - LICENSE
  - justfile
  - .golangci.yml
autonomous: true
requirements:
  - OSS-01
  - OSS-05
  - OSS-09
must_haves:
  truths:
    - "`go mod tidy` completes without errors"
    - "`just test` runs `go test ./...` successfully (even with no test files yet)"
    - "`just build` compiles the module root package"
    - "`just lint` runs golangci-lint without config errors"
    - "MIT LICENSE file exists with correct copyright"
  artifacts:
    - path: "go.mod"
      provides: "Module declaration with path github.com/tbcrawford/drift"
      contains: "module github.com/tbcrawford/drift"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "justfile"
      provides: "Developer task runner"
      contains: "test"
    - path: ".golangci.yml"
      provides: "Linter configuration"
  key_links:
    - from: "go.mod"
      to: "github.com/tbcrawford/drift"
      via: "module declaration"
      pattern: "module github\\.com/tbcrawford/drift"
---

<objective>
Bootstrap the Go module: create go.mod at module root `github.com/tbcrawford/drift`, MIT LICENSE, justfile with common dev tasks, and golangci-lint config.

Purpose: Everything in Phase 1 depends on the module existing with the correct import path. The justfile tasks (`just test`, `just lint`, `just build`, `just bench`) are the canonical way to run the project and must exist from the start.

Output: A compilable Go module at `github.com/tbcrawford/drift` with developer tooling ready.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Stack constraints from STACK.md (pre-Phase 1 decisions): -->
<!-- Module path: github.com/tbcrawford/drift -->
<!-- Go version: 1.21 (generics, min/max builtins, slices package) -->
<!-- NO Bubble Tea v2 until v2.0 of drift — do not add to go.mod -->
<!-- Lip Gloss v2 import path: charm.land/lipgloss/v2 (NOT github.com/charmbracelet/lipgloss) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize go.mod and create MIT LICENSE</name>
  <files>go.mod, go.sum, LICENSE</files>
  <action>
    Run `go mod init github.com/tbcrawford/drift` to create go.mod with module path `github.com/tbcrawford/drift` and Go version `1.21`.

    Create `LICENSE` with MIT license text. Copyright holder: "Tyler Crawford". Year: 2026.

    Do NOT add any dependencies yet — go.mod starts clean. Dependencies are added in subsequent plans when code that needs them is written.
  </action>
  <verify>
    <automated>go mod verify && grep -q 'module github.com/tbcrawford/drift' go.mod && grep -q 'go 1.21' go.mod && grep -q 'MIT License' LICENSE</automated>
  </verify>
  <done>go.mod has correct module path and Go version; LICENSE contains MIT text with Tyler Crawford copyright</done>
</task>

<task type="auto">
  <name>Task 2: Create justfile and golangci-lint config</name>
  <files>justfile, .golangci.yml</files>
  <action>
    Create `justfile` (no extension — just recipe file) with these recipes:
    ```
    # Default: show available recipes
    default:
        @just --list

    # Run all tests
    test:
        go test ./...

    # Run tests with race detector
    test-race:
        go test -race ./...

    # Run benchmarks
    bench:
        go test -bench=. -benchmem ./...

    # Build CLI binary
    build:
        go build ./cmd/drift/...

    # Run linter
    lint:
        golangci-lint run ./...

    # Run go vet
    vet:
        go vet ./...

    # Tidy dependencies
    tidy:
        go mod tidy

    # Run property-based tests with verbose output
    test-property:
        go test -v -run TestProperty ./...

    # Fuzz the Myers algorithm (60s budget)
    fuzz:
        go test -fuzz=FuzzMyers -fuzztime=60s ./internal/algo/myers/...
    ```

    Create `.golangci.yml` enabling: `govet`, `staticcheck`, `errcheck`, `unused`, `gosimple`, `ineffassign`. Set `go: "1.21"`. Exclude test files from `errcheck`. Set `run.timeout: "5m"`.

    Example `.golangci.yml`:
    ```yaml
    run:
      timeout: 5m
      go: "1.21"

    linters:
      enable:
        - govet
        - staticcheck
        - errcheck
        - unused
        - gosimple
        - ineffassign

    issues:
      exclude-rules:
        - path: "_test.go"
          linters:
            - errcheck
    ```
  </action>
  <verify>
    <automated>just --list 2>/dev/null | grep -q 'test' && test -f .golangci.yml && grep -q 'govet' .golangci.yml</automated>
  </verify>
  <done>justfile has test/build/lint/bench recipes; .golangci.yml enables govet, staticcheck, errcheck, unused</done>
</task>

</tasks>

<verification>
- `go mod verify` passes
- `go.mod` declares `module github.com/tbcrawford/drift` and `go 1.21`
- `LICENSE` contains "MIT License" and "Tyler Crawford"
- `just --list` shows all recipes without error
- `.golangci.yml` is valid YAML with expected linters listed
</verification>

<success_criteria>
Module is bootstrapped: correct import path, MIT license, developer task runner, lint config. A `go get github.com/tbcrawford/drift` from an external module would resolve (once code exists).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
