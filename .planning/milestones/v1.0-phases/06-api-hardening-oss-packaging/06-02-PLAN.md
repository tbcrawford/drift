---
plan: "06-02"
title: "Godoc audit — full coverage for exported API"
wave: 2
depends_on: ["06-01"]
files_modified:
  - "drift.go"
  - "render.go"
  - "options.go"
  - "types.go"
  - "builder.go"
  - "doc.go"
requirements_addressed: ["OSS-02"]
autonomous: true
---

## Objective

Ensure every exported identifier in `github.com/tbcrawford/drift` has a complete godoc comment per Go conventions (starts with the name, states purpose). After Phase 6-01, this includes `Builder` and `New`. Satisfy OSS-02 and ROADMAP success criterion #2.

## must_haves

- [ ] `go doc -all github.com/tbcrawford/drift` shows no exported symbol with an empty doc (every type, func, const, var has a non-blank doc comment block)
- [ ] Algorithm constants (`Myers`, `Patience`, `Histogram`) each have a one-line comment if not already sufficient as a group

## Tasks

<task id="06-02-01" title="Enumerate exports and add or fix comments">
<read_first>
- doc.go — current package doc
- types.go — exported aliases and constants
- options.go — `Option`, `With*` functions
- drift.go — `Diff`
- render.go — `Render`, `RenderWithNames`, document whether `resolveProfile` stays unexported (no comment required)
- builder.go — from plan 06-01
</read_first>

<action>
**Step 1:** From repo root, list exported symbols:

```bash
go doc -all github.com/tbcrawford/drift 2>&1 | head -200
```

Also run:

```bash
go list -f '{{.Dir}}' github.com/tbcrawford/drift
```

**Step 2:** For each exported name in these files — `doc.go`, `types.go`, `options.go`, `drift.go`, `render.go`, `builder.go` — verify a doc comment immediately precedes the declaration. Add missing comments for:

- `Builder` type and every exported method on `Builder`
- `New`
- Any exported `Op` / `Edit` / line-related identifiers lacking full sentences

**Step 3:** Expand `doc.go` package overview to include a **second** quick-start block showing builder usage (3–6 lines), preserving the existing functional example.

**Step 4:** Run `go fmt ./...` and `go test ./...`.
</action>

<acceptance_criteria>
- `go doc github.com/tbcrawford/drift` exits 0 and output contains substring `Package drift`
- `go doc github.com/tbcrawford/drift Builder` exits 0 and first line of output starts with `type Builder`
- `go vet ./...` exits 0
- `go test ./...` exits 0
- `grep -E '^func \(b \*Builder\)' builder.go | wc -l` outputs a number ≥ 6 (Diff, Render, RenderWithNames, plus at least four chain methods — adjust grep if method names differ, but every exported `Builder` method must have a comment; verify manually in file that no `^func (b *Builder)` lacks a preceding `//` comment block)
</acceptance_criteria>
</task>

## Verification criteria

- `staticcheck ./...` passes if present in `just lint` / CI
- No exported symbol omitted from documentation pass
