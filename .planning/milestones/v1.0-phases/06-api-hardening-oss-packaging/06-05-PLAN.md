---
plan: "06-05"
title: "README — installation, CLI, APIs, rendering"
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - "README.md"
requirements_addressed: ["OSS-06"]
autonomous: true
---

## Objective

Author or replace root `README.md` so it documents installation (`go get` / `go install`), CLI usage, functional library API, builder API, and rendering options (unified vs split, theme/lang), each with at least one fenced code block. Satisfy OSS-06 and ROADMAP success criterion #5.

## must_haves

- [ ] README contains distinct sections (headings) covering: Installation, CLI usage, Library (functional), Library (builder), Rendering
- [ ] Each section includes a ``` go code fence (or ```bash for CLI) with a minimal copy-paste example
- [ ] README mentions `go install github.com/tbcrawford/drift/cmd/drift@latest`

## Tasks

<task id="06-05-01" title="Write README.md aligned with actual flags and API">
<read_first>
- cmd/drift/main.go — exact flag names and usage string
- drift.go, render.go, builder.go — final public API
- examples/basic/main.go and examples/builder/main.go — shorten or adapt for README snippets
- .planning/PROJECT.md — one-line project pitch (optional tone)
</read_first>

<action>
**Step 1:** Create or overwrite `README.md` with this structure (heading text may vary slightly but content must exist):

1. **Title + tagline** — project name `drift`, one sentence on value prop
2. **Installation** — `go get github.com/tbcrawford/drift@latest` and `go install github.com/tbcrawford/drift/cmd/drift@latest`
3. **CLI usage** — fenced block showing at least: `drift file1.go file2.go` and one of `--split` or `--algorithm` (match real flags from Cobra)
4. **Library — functional API** — fenced Go block: `drift.Diff` + `drift.Render` with error handling
5. **Library — builder API** — fenced Go block: `drift.New().Algorithm(...).Diff(...)` matching compiled API
6. **Rendering** — explain unified (default) vs `WithSplit()`, mention `WithTheme` and `WithLang` in prose + optional small code fence
7. **License** — MIT, link to `LICENSE`

**Step 2:** Do not invent flags; if a flag is not implemented, document only what exists in `cmd/drift/main.go` at execution time.

**Step 3:** Screenshots are optional; if omitted, add a short note that output is ANSI-colored in terminal.
</action>

<acceptance_criteria>
- `test -f README.md` exits 0
- `grep -q 'go install github.com/tbcrawford/drift/cmd/drift' README.md` exits 0
- `grep -q '```' README.md` exits 0 (must contain fenced blocks — verify at least 4 opening fences)
- `grep -qiE 'builder|New\(\)' README.md` exits 0
- `grep -qi split README.md` exits 0
- `grep -qiE 'Diff\(|Render\(' README.md` exits 0
</acceptance_criteria>
</task>

## Verification criteria

- README examples compile when pasted into a file with `module` depending on drift (sanity-check by matching imports to `github.com/tbcrawford/drift`)
