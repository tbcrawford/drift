---
plan: "05-04"
title: "Integration tests and go install verification for cmd/drift"
wave: 4
depends_on: ["05-03"]
files_modified:
  - "go.mod"
  - "go.sum"
  - "cmd/drift/cli_test.go"
  - "cmd/drift/testdata/script/*.txt"
requirements_addressed: ["OSS-04"]
autonomous: true
---

## Objective

Add `github.com/rogpeppe/go-internal` as a **test-only** dependency (`go get github.com/rogpeppe/go-internal@v1.14.1` in a test file with `//go:build` or use `_test.go` import so it is not linked into the binary). Use `testscript` to run the real `drift` binary against stdin, files, flags, and assert exit codes. Add a test that runs `go build -o drift.testbin ./cmd/drift` in a temp dir and executes scripts. Document OSS-04 manual verification for post-publish `go install github.com/tylercrawford/drift/cmd/drift@latest`.

## must_haves

- [ ] At least **3** `testscript` files under `cmd/drift/testdata/script/` covering: (1) two files differ → exit 1 + stdout contains `@@`, (2) identical inputs → exit 0 + empty or no `@@` per renderer, (3) `--no-color` or `--algorithm histogram` path executes successfully
- [ ] `go test ./cmd/drift/...` runs testscript and exits 0
- [ ] `just build` exits 0

## Tasks

<task id="05-04-01" title="Add testscript harness and OSS-04 verification docs in plan summaries">
<read_first>
- cmd/drift/main.go — final `rootCmd` Use line and flag names (scripts must match)
- go.mod — module path `github.com/tylercrawford/drift` for install instruction
- .planning/phases/02-algorithms/02-RESEARCH.md or project docs — optional reference for algorithm names
</read_first>

<action>
**Step 1:** Add test dependency:

```bash
go get github.com/rogpeppe/go-internal@v1.14.1
```

Import in `cmd/drift/cli_test.go` only:

```go
func TestMain(m *testing.M) {
  os.Exit(testscript.RunMain(m, map[string]func() int{
    "drift": main1,
  }))
}
```

Where `main1` is a wrapper that calls the same `rootCmd.Execute()` entry as `main` (extract `func executeDrift() int` from `main.go` returning exit code so `RunMain` can invoke it).

**Refactor requirement:** `main()` becomes `os.Exit(executeDrift())` and `executeDrift()` returns int exit code (0,1,2) so testscript can link `drift` command without `os.Exit` in tests killing the runner.

**Step 2:** Add `func TestScript(t *testing.T) { testscript.Run(t, testscript.Params{Dir: "testdata/script"}) }`.

**Step 3:** Create scripts (exact content examples — adjust paths to match test env):

`testdata/script/basic.txtar` or separate `.txt` files per testscript convention — use `testscript` txtar format:

```
exec drift $WORK/a $WORK/b
stderr .
stdout @@
! stdout .
```

Use `$WORK` for temp files; in script preamble create files with `cp` or `exec` heredoc per testscript docs.

**Minimum scripts:**

1. `diff-exit1.txt` — two different files → `cmp stderr` empty, `stdout` contains substring `@@` (unified hunk header) OR `+`/`-` lines
2. `diff-exit0.txt` — identical files → `! stdout @@` OR check exit 0 with `cmp stdout golden` empty output
3. `flags.txt` — `drift --algorithm myers --from a --to b` with a≠b → exit 1

**Step 4:** Ensure `testscript` sets PATH to compiled binary — follow https://pkg.go.dev/github.com/rogpeppe/go-internal/testscript pattern for `RunMain`.

**Step 5:** Append to this phase’s `05-04-SUMMARY.md` after execution (executor creates summary) a bullet: **OSS-04 manual:** After module is published with tag, run `go install github.com/tylercrawford/drift/cmd/drift@vX.Y.Z` and `drift --help`.

For planning artifact only: add a **comment block** at bottom of `05-04-PLAN.md` is wrong — put manual steps in task `<acceptance_criteria>` instead.

</action>

<acceptance_criteria>
- `grep -q 'rogpeppe/go-internal' go.mod` exits 0
- `test -f cmd/drift/cli_test.go` exits 0
- `find cmd/drift/testdata/script -name '*.txt' -o -name '*.txtar' | wc -l` outputs integer ≥ 3 (use `find ... | wc -l` and verify ≥ 3 in executor notes; if using single txtar, count files inside)
- `go test ./cmd/drift/...` exits 0
- `just build` exits 0
</acceptance_criteria>
</task>

## Verification criteria

- OSS-04: automated local substitute via `go build ./cmd/drift`; manual `go install …@latest` documented in executor summary post-plan
