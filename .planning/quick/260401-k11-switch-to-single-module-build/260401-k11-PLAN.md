---
phase: quick
plan: 260401-k11
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - go.work
  - go.work.sum
  - cmd/drift/go.mod
  - cmd/drift/go.sum
  - .github/workflows/ci.yml
  - .github/workflows/security.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "`go test -race ./...` from repo root exits 0 (all library and CLI tests pass)"
    - "`go build ./cmd/drift` from repo root produces a binary"
    - "No go.work, go.work.sum, cmd/drift/go.mod, or cmd/drift/go.sum files remain"
    - "CI no longer references the CLI sub-module step"
  artifacts:
    - path: "go.mod"
      provides: "Single module declaration with all deps (library + CLI)"
      contains: "github.com/spf13/cobra"
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI without duplicate sub-module test step"
  key_links:
    - from: "cmd/drift/main.go"
      to: "github.com/tbcrawford/drift"
      via: "same-module import (no replace directive needed)"
---

<objective>
Collapse the two-module layout (root library + `cmd/drift` sub-module linked by `go.work`) into a single `go.mod` at the repo root. The CLI entry point stays at `cmd/drift/` and `go install github.com/tbcrawford/drift/cmd/drift@latest` continues to work.

Purpose: Eliminate workspace complexity; single `go test -race ./...` from root covers everything; standard importers get a clean single-module library.
Output: One `go.mod`, no `go.work`, passing tests, updated CI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260401-k11-switch-to-single-module-build/260401-k11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge modules — delete workspace files and absorb CLI deps into root go.mod</name>
  <files>go.mod, go.sum, go.work, go.work.sum, cmd/drift/go.mod, cmd/drift/go.sum</files>
  <action>
Perform these steps in order:

1. **Delete workspace files** — remove `go.work` and `go.work.sum` from the repo root.

2. **Delete CLI sub-module files** — remove `cmd/drift/go.mod` and `cmd/drift/go.sum`.

3. **Add CLI-only direct deps to root go.mod** — the following packages are direct deps of `cmd/drift` but are NOT yet in the root `go.mod` direct section:
   - `github.com/rogpeppe/go-internal v1.14.1`
   - `github.com/spf13/cobra v1.9.1`

   Add them to the `require` block alongside the existing direct deps. Do NOT add `github.com/tbcrawford/drift` (the self-referencing replace directive disappears entirely).

   The indirect transitives (`github.com/inconshreveable/mousetrap`, `github.com/spf13/pflag`, `golang.org/x/tools`) will be reconciled by `go mod tidy` in the next step.

4. **Tidy** — run `go mod tidy` from the repo root to reconcile `go.sum`, promote/demote indirect deps, and remove the now-deleted sub-module's lock entries.

5. **Verify build compiles** — run `go build ./cmd/drift` to confirm the CLI package resolves correctly within the single module.

Key constraint: `cmd/drift/main.go` already imports `github.com/tbcrawford/drift` (the library) using the correct module path — no import path changes needed in any `.go` file.
  </action>
  <verify>
    <automated>go build ./cmd/drift && go test -race ./... && echo "PASS"</automated>
  </verify>
  <done>Single go.mod at repo root contains cobra and go-internal as direct deps; go.work and cmd/drift/go.mod are deleted; `go test -race ./...` exits 0 from repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflows to remove sub-module step and fix security scan path</name>
  <files>.github/workflows/ci.yml, .github/workflows/security.yml</files>
  <action>
**ci.yml** — Remove the now-redundant "Test (CLI sub-module explicit)" step (the one with `working-directory: cmd/drift`). The single `go test -race ./...` step already covers everything. Also rename the remaining test step label from "Test (library + CLI via go.work)" to "Test" since go.work no longer exists.

The final `test` job steps should be:
1. Checkout
2. Set up Go
3. Vet (`go vet ./...`)
4. Test (`go test -race ./...`)
5. Lint (golangci-lint, stable only)

**security.yml** — The `go-package` value is currently `github.com/tbcrawford/drift/...`. With a single module this is correct and needs no change. However, verify the step doesn't reference any workspace flag. The existing `golang/govulncheck-action@v1` invocation is fine as-is — no edits needed to security.yml unless a workspace flag was present.

Note: `release.yml` does not need changes — goreleaser already references `./cmd/drift` as the main entry point and works from the repo root.
  </action>
  <verify>
    <automated>grep -c "working-directory: cmd/drift" .github/workflows/ci.yml && echo "FAIL: sub-module step still present" || echo "PASS: sub-module step removed"</automated>
  </verify>
  <done>ci.yml has exactly one test step (no `working-directory: cmd/drift`); the test step runs `go test -race ./...` from repo root; security.yml unchanged or confirmed correct.</done>
</task>

</tasks>

<verification>
From repo root:

```sh
# No workspace files remain
ls go.work 2>/dev/null && echo FAIL || echo PASS

# No sub-module files remain  
ls cmd/drift/go.mod 2>/dev/null && echo FAIL || echo PASS

# CLI deps present in root go.mod
grep "spf13/cobra" go.mod && grep "rogpeppe/go-internal" go.mod

# Full test suite passes
go test -race ./...

# CLI builds correctly
go build ./cmd/drift

# CI has no sub-module step
grep -c "working-directory: cmd/drift" .github/workflows/ci.yml  # must print 0
```
</verification>

<success_criteria>
- `go.work`, `go.work.sum`, `cmd/drift/go.mod`, `cmd/drift/go.sum` are all deleted
- Root `go.mod` declares `github.com/spf13/cobra` and `github.com/rogpeppe/go-internal` as direct dependencies
- `go test -race ./...` from repo root exits 0
- `go build ./cmd/drift` from repo root exits 0
- `.github/workflows/ci.yml` has no `working-directory: cmd/drift` step
- `go install github.com/tbcrawford/drift/cmd/drift@latest` would work (single module, standard `cmd/` layout)
</success_criteria>

<output>
After completion, create `.planning/quick/260401-k11-switch-to-single-module-build/260401-k11-SUMMARY.md` with what was done and any notable decisions.
</output>
