---
quick_task: 260401-k11
subsystem: module-layout
tags: [go-modules, ci, single-module, workspace-removal]
dependency_graph:
  requires: []
  provides: [single-module-build, cmd/drift-importable]
  affects: [go.mod, go.sum, ci.yml]
tech_stack:
  patterns: [single-go-module, cmd-layout, go-install]
key_files:
  modified:
    - go.mod
    - go.sum
    - .github/workflows/ci.yml
  deleted:
    - go.work
    - go.work.sum
    - cmd/drift/go.mod
    - cmd/drift/go.sum
decisions:
  - "Deleted go.work/go.work.sum; absorbed cobra and go-internal as direct root deps"
  - "security.yml unchanged — go-package path github.com/tbcrawford/drift/... correct for single module"
metrics:
  duration: "~5 minutes"
  completed: "2026-04-01T18:41:48Z"
  tasks: 2
  commits: 2
---

# Quick Task 260401-k11: Switch to Single Module Build — Summary

**One-liner:** Collapsed two-module (go.work workspace) layout into a single `go.mod` — cobra and go-internal absorbed as direct deps, workspace files deleted, CI de-duplicated.

## What Was Done

### Task 1: Merge modules (commit `41ea963`)

Deleted the four workspace/sub-module files:
- `go.work` — no longer needed; single module resolves without workspace
- `go.work.sum` — deleted with go.work
- `cmd/drift/go.mod` — CLI is now part of root module, not a sub-module
- `cmd/drift/go.sum` — deleted with cmd/drift/go.mod

Added the two CLI-only direct deps that were missing from root `go.mod`:
- `github.com/rogpeppe/go-internal v1.14.1` — CLI integration testing (testscript)
- `github.com/spf13/cobra v1.9.1` — CLI framework

Ran `go mod tidy` to reconcile `go.sum`, promote/demote indirect deps, and remove the deleted sub-module's lock entries.

`cmd/drift/main.go` already imports `github.com/tbcrawford/drift` using the correct module path — no `.go` file changes were needed. The replace directive disappeared along with `cmd/drift/go.mod`.

**Verification:** `go build ./cmd/drift` ✓ and `go test -race ./...` (230 tests, 16 packages) ✓

### Task 2: Update CI (commit `a9ca06d`)

**ci.yml:**
- Removed redundant "Test (CLI sub-module explicit)" step that used `working-directory: cmd/drift`
- Renamed "Test (library + CLI via go.work)" → "Test" (go.work no longer exists)
- Final test job: Checkout → Set up Go → Vet → Test → Lint (stable only)

**security.yml:** No changes needed. The `go-package: github.com/tbcrawford/drift/...` value is correct for a single module and contained no workspace flags.

## Deviations from Plan

None — plan executed exactly as written.

## Success Criteria Verification

| Criterion | Result |
|-----------|--------|
| `go.work` deleted | ✓ |
| `go.work.sum` deleted | ✓ |
| `cmd/drift/go.mod` deleted | ✓ |
| `cmd/drift/go.sum` deleted | ✓ |
| Root `go.mod` has `github.com/spf13/cobra` as direct dep | ✓ |
| Root `go.mod` has `github.com/rogpeppe/go-internal` as direct dep | ✓ |
| `go test -race ./...` exits 0 (230 tests pass) | ✓ |
| `go build ./cmd/drift` exits 0 | ✓ |
| `ci.yml` has no `working-directory: cmd/drift` step | ✓ |
| `go install github.com/tbcrawford/drift/cmd/drift@latest` pattern valid | ✓ (standard cmd/ layout, single module) |

## Known Stubs

None.

## Self-Check: PASSED

- `go.mod` exists and contains cobra + go-internal: ✓
- `go.work` does not exist: ✓
- `cmd/drift/go.mod` does not exist: ✓
- Commits `41ea963` and `a9ca06d` exist in git log: ✓
- 230 tests pass with `-race`: ✓
